const arrayNumeros = [12, 22, 78, 45, 2, 3, 8, 10]

const triplos = arrayNumeros.map((numeros) => {

return numeros * 3
   })
console.log(triplos)


const metades = arrayNumeros.map((numeros) => {

    return numeros / 2
       })
    console.log(metades)